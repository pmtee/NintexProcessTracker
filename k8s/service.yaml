apiVersion: v1
kind: Service
metadata:
  name: processtracker-svc
  namespace: processtracker
spec:
  selector:
    app: processtracker
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
